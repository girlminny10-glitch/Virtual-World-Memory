[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["nodejs"]

[start]
cmd = "pnpm install && pnpm run typecheck && pnpm --filter @workspace/api-server run build && node --enable-source-maps ./artifacts/api-server/dist/index.mjs"
